import { HighlightImageDirective } from './highlight-image.directive';

describe('HighlightImageDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightImageDirective();
    expect(directive).toBeTruthy();
  });
});
